# 工作区文件整理说明

## 整理日期
2025-11-12

## 整理内容

### 1. 创建新目录

- **scripts/** - 存放所有工具脚本和辅助文件
- **temp/** - 存放临时文件和测试文件

### 2. 文件移动记录

#### 脚本文件 → `scripts/`
- `fix_stage_names.js` - 修复阶段名称脚本
- `fix_streamlit_cloudbase.py` - 修复 Streamlit CloudBase 配置
- `permission_system_guide.py` - 权限系统指南脚本
- `build_static.py` - 构建静态文件脚本
- `cloud_hosting_package.py` - 云托管打包脚本
- `create_lightweight_package.py` - 创建轻量级打包脚本
- `create_upload_package.py` - 创建上传打包脚本

#### 测试文件 → `tests/`
- `test_*.py` (8个文件) - Python 测试文件
- `test_*.js` (1个文件) - JavaScript 测试文件

#### 文档文件 → `docs/`
- `CLOUDBASE_DEPLOY_GUIDE.md` - CloudBase 部署指南
- `upload_deploy_guide.md` - 上传部署指南
- `websocket_fix_guide.md` - WebSocket 修复指南

#### 临时文件 → `temp/`
- `test.jpg` - 测试图片
- `photo_0.jpg` - 临时照片

### 3. 目录结构

```
life-diamond-system/
├── scripts/          # 工具脚本
├── tests/            # 测试文件
├── docs/             # 文档文件
├── temp/             # 临时文件
├── streamlit_app/    # Streamlit 应用
├── cloudbase_functions/  # 云函数
├── archive/          # 归档文件
└── ...
```

### 4. .gitignore 更新

已更新 `.gitignore` 文件，反映新的文件结构：
- 注释了已移动到新目录的文件规则
- 添加了 `temp/` 目录的忽略规则
- 保留了敏感配置文件的忽略规则

### 5. 注意事项

1. **敏感文件**：`SecretKey.csv` 和 `tencent_config.py` 仍在根目录，已在 `.gitignore` 中忽略
2. **临时文件**：`temp/` 目录中的文件可能会被定期清理
3. **测试文件**：所有测试文件已统一移动到 `tests/` 目录
4. **脚本文件**：所有工具脚本已统一移动到 `scripts/` 目录，并添加了 README 说明

### 6. 后续建议

1. 定期清理 `temp/` 目录
2. 考虑将敏感配置文件移动到更安全的位置
3. 为 `scripts/` 目录中的脚本添加更详细的文档说明
4. 考虑将 `archive/` 目录中的内容归档到外部存储

